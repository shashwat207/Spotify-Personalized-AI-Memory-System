"""
MCP Resources — read-only context an MCP host can fetch by URI, as
opposed to tools which perform an action. Good for handing the LLM a
user's profile/preferences as ambient context without it needing to
decide to call a tool first.
"""
