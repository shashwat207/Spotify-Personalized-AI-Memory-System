"""
Reusable, parametrized Cypher strings as Python constants — imported
by services/repositories that don't warrant their own repository
method. These mirror the .cypher files under graph/cypher/queries/
(kept there for manual running in Neo4j Browser / Desktop).
"""
